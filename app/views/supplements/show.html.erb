<% content_for :head do %>
  <title><%= @supplement.name %> Supplement: Benefits, Uses and Side Effects | Name</title>
  <meta name="description" content="website description < 300 chars">
<% end %>

<nav aria-label="Breadcrumb" class="breadcrumb">
  <ol>
    <li>
      <a href="<%= root_path %>">Home</a>
    </li>
    <li>
      <a href="<%= supplements_path %>">Supplements</a>
    </li>
    <li>
      <a href="<%= category_path(@category) %>"><%= @category.description%></a>
    </li>
    <li>
      <a href="<%= url_for(:only_path => false) %>"><%= @supplement.name %></a>
    </li>
  </ol>
</nav>

<p>
  <strong>ID:</strong>
  <%= @supplement.id %>
</p>

<p>
  <strong>Name:</strong>
  <%= @supplement.name %>
</p>
 
<p>
  <strong>AKA:</strong>
  <%= @supplement.aka %>
</p>

<% if @supplement.category_id != nil %>

  <p>
    <strong>Category:</strong>
    <%= Category.find(@supplement.category_id).description %>
  </p>

<% end %>